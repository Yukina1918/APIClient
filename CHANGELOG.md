# Changelog

## [v5.0] - 2026-09-24

### ✨ 新增主要功能
1. **独立代码文本区块，带一键复制按钮**
- AI输出的代码块自动渲染为独立UI区块；
- 右上角操作图标：一键复制、HTML预览（浏览器打开）、全屏查看；
- 完整适配亮色 / 暗色双主题，跟随全局字体设置、支持多语言UI。

2. **本地办公Agent沙箱（强制锁定工作目录）**
- 设置页面新增「本地办公」配置面板，可自由选择本地工作路径；
- **代码硬级别路径防护**：拦截 `../` 目录逃逸、绝对路径、符号链接、跨盘符访问；示例输入 `../bnbwe/weqwehb` 将直接拒绝，模型提示词无法绕过；
- 文件读写、Shell命令执行能力**默认全部关闭**，需要用户手动打开开关；
- Shell 命令内置白名单；遇到未授权命令会弹窗申请权限：允许一次 / 永久加入白名单 / 拒绝；
- 全部文件IO严格限制在选定的办公沙箱目录之内。

3. **会话存储结构重构（文件夹式会话）**
聊天记录由单 txt 文件改为会话文件夹存储：
- 会话ID格式：`[年月日‑时分]_[24位英数混合随机数]`
- 每一个会话对应独立文件夹，内部自动建立分类子目录：
  - `chat/`：聊天原始文本记录
  - `txt/`、`json/`、`py/`、`java/`、`html/`：不同类型输出文件分类存放
  - `screenshots/`：存放「AI眼睛」截图图像
- 向下兼容，可正常读取旧版本单文件历史聊天记录，平滑升级。

4. **AI眼睛 — 多模态截图视觉能力**
- 内置 `vision.py`；Windows 使用 ctypes + GDI 原生截屏，**不依赖第三方图像库**；Linux / macOS 调用系统截图工具；
- 支持全屏截图、导入本地图片，编码为 base64 提交给多模态大模型；
- 需要配置支持视觉的模型：豆包视觉、DeepSeek‑Flash、Qwen‑VL 等；
- 截图自动保存到当前会话下 `screenshots/` 子目录，受沙箱路径管控。

5. **帮助/教程页面扩展**
- 帮助窗口新增页签：`本地办公`、`AI眼睛`；
- 包含完整使用步骤、参数说明、安全警告；
- FAQ 同步更新，支持中英双语。

### 🐛 Bug 修复
- 修复聊天页停止按钮缺失 `on_stop` 方法，启动直接抛出 `AttributeError` 崩溃；
- 完善停止生成逻辑：点击停止按钮设置停止事件，Agent后台线程安全退出，UI状态正确重置；
- 全局扫描全部UI类，排查引用未定义成员的潜在运行时异常。

### ⚠️ 安全重要提示
> 开启本地文件、Shell命令工具存在安全风险，请仔细阅读！
1. 文件操作、命令执行工具出厂**默认关闭**，必须由用户手动在设置内开启；
2. 建议使用独立磁盘分区/文件夹作为沙箱工作目录，**不要把个人重要文档放入工作目录**；
3. 如果部署在云电脑：不要存放敏感隐私文件，仅用于Agent任务运行；
4. 大模型系统提示仅做辅助约束，安全以代码层路径校验为准，**不可信任模型输出的路径字符串**。

### 📦 运行启动
```bash
# 方式一
python run.py

# 方式二
python -m ai_chat
