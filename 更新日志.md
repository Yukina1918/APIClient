# Changelog

## [v5.2] - 2026-09-25

### ✨ 主要更新
1. **模型选择下拉框修复**
   - 恢复可编辑 QComboBox：可从内置预设选择模型，也可手动输入模型名；
   - 原生下拉箭头在 QSS 下不显示的问题，改为文字符号「▼」右对齐叠加，下拉展开时自动变为「▲」。

2. **图灵识别（原「AI 眼睛」）体验改进**
   - 右上角按钮更名为「图灵识别」，聊天设置、教程、视觉弹窗文案全部统一；
   - 发送图片后聊天区立即显示图片预览；
   - 图片持久化到 `data/chat/{会话}/images/`，加载历史会话时恢复预览；
   - 图片采用视口剔除优化：滚出可视区域释放显存，滚回时自动恢复，不再一刀切不渲染历史图片；
   - 修复 Ctrl+V 粘贴纯文本时误弹「剪贴板里没有图片」的提示，粘贴文字现在静默放行。

3. **界面与主题**
   - 主题切换从下拉框改为按钮点击切换（亮色 ↔ 暗色），按钮实时显示「当前 → 目标」；
   - 彩色 Emoji 跟随系统：字体回退到 Segoe UI Emoji，Win10 / Win11 自动使用系统表情；
   - 窗口 / 任务栏 / exe 文件图标替换为自定义图标。

4. **Markdown 渲染增强**
   - Markdown 表格现在渲染为带边框的真正表格（无论模型是否用 ``` 包裹），不再显示成纯文本或代码块。

5. **聊天记录格式与存储**
   - 自定义 txt 记录格式：`Me=` / `MeAttachment=` / `AI=` / `System=`；
   - 会话存储改为单文件夹结构：`data/chat/{id}/{id}.txt` + `images/`，旧版平铺记录自动迁移。

6. **修复**
   - 修复聊天设置弹窗大量文案直接显示 i18n key（如 `cs.vision_enable`）的问题，补齐缺失翻译；
   - 修复若干启动期 `AttributeError` / `ImportError`。

### 📦 打包
- 提供单文件 `AIClient5.2.exe`，自带 Python + PyQt6 运行环境，无控制台，双击即用。

---

## [v5.0] - 2026-09-24

### ✨ 新增主要功能
1. **独立代码文本区块，带一键复制按钮**
   - AI 输出的代码块自动渲染为独立 UI 区块；
   - 右上角操作图标：一键复制、HTML 预览（浏览器打开）、全屏查看；
   - 完整适配亮色 / 暗色双主题，跟随全局字体设置、支持多语言 UI。

2. **本地办公 Agent 沙箱（强制锁定工作目录）**
   - 设置页新增「本地办公」配置面板，可自由选择本地工作路径；
   - **代码硬级别路径防护**：拦截 `../` 目录逃逸、绝对路径、符号链接、跨盘符访问；
   - 文件读写、Shell 命令执行能力**默认全部关闭**，需要用户手动打开；
   - Shell 命令内置白名单；遇到未授权命令弹窗申请权限：允许一次 / 永久加入白名单 / 拒绝；
   - 全部文件 IO 严格限制在选定的办公沙箱目录之内。

3. **会话存储结构重构（文件夹式会话）**
   - 每个会话对应独立文件夹，内部自动建立分类子目录：chat、txt、json、py、java、html、screenshots；
   - 向下兼容旧版本单 txt 聊天记录，平滑升级。

4. **图灵识别 — 多模态截图视觉能力**
   - Windows 使用 ctypes + GDI 原生截屏，**不依赖第三方图像库**；Linux / macOS 调用系统截图工具；
   - 支持全屏截图、导入本地图片，编码为 base64 提交给多模态模型；
   - 截图自动保存到当前会话下 `screenshots/` 子目录，受沙箱路径管控。

5. **帮助 / 教程页面扩展**
   - 帮助窗口新增页签：「本地办公」「图灵识别」；
   - 包含完整使用步骤、参数说明、安全警告；FAQ 同步更新，支持多语言。

### ⚠️ 安全重要提示
> 开启本地文件、Shell 命令工具存在安全风险：
1. 文件操作、命令执行工具出厂**默认关闭**，必须由用户手动在设置内开启；
2. 建议使用独立磁盘分区 / 文件夹作为沙箱工作目录，**不要把个人重要文档放入工作目录**；
3. 大模型系统提示仅做辅助约束，安全以代码层路径校验为准。
